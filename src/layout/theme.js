import { colors } from './variables'
import { flexCenter, flexStart, flexSpaceBeetwen, flexColumn } from './mixins'

export const theme = {
  colors,
  flexCenter,
  flexStart,
  flexSpaceBeetwen,
  flexColumn
}
